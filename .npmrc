# Fail fast when the local Node/npm version does not match "engines".
engine-strict=true

# Exact versions in the lockfile only; ranges stay in package.json so security
# patches can be picked up by a deliberate `npm update`.
save-exact=false

# Reproducible installs: never silently reach for a newer transitive version.
fund=false
audit=false
